version: '3.7'

services:
  ui:
    build: ./
    command: npm run dev
    volumes:
      - ./:/app
      - node_modules:/app/node_modules
    ports:
      - 8080:8080
    environment:
      - NODE_HOST=10.13.0.1

volumes:
  node_modules:
